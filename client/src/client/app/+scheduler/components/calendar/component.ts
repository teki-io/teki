import {Component, OnChanges, Input} from 'angular2/core';
import * as moment from 'moment';

@Component({
  selector: 'calendar',
  templateUrl: 'app/+scheduler/components/calendar/component.html'
})

export class Calendar {
  constructor() {}
}
